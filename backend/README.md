# Backend API - Бизнес-Помощник (Node.js)

Express.js backend для AI-помощника малого бизнеса.

## Установка

1. Убедитесь, что установлен Node.js 18+:
```bash
node --version
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env`:
```bash
OPENAI_API_KEY=your_openai_api_key_here
PORT=8000
NODE_ENV=development
```

4. Запустите сервер:
```bash
# Режим разработки (с автоперезагрузкой)
npm run dev

# Или в продакшн режиме
npm start
```

Сервер будет доступен на `http://localhost:8000`

## Структура проекта

```
backend/
├── server.js              # Главный файл сервера
├── config.js              # Конфигурация
├── package.json           # Зависимости
├── routes/                # API маршруты
│   ├── index.js
│   ├── chat.js            # Чат с AI
│   ├── categories.js      # Категории
│   ├── analyze.js         # Анализ данных
│   └── content.js         # Генерация контента
├── services/              # Сервисы
│   └── openai.js         # OpenAI утилиты
└── constants/             # Константы
    └── prompts.js         # Системные промпты
```

## API Endpoints

### GET `/`
Информация о API

### GET `/health`
Проверка здоровья сервиса

### GET `/categories`
Получить список доступных категорий

**Ответ:**
```json
{
  "categories": [
    {"id": "general", "name": "Общие вопросы", "icon": "💼"},
    ...
  ]
}
```

### POST `/chat`
Отправить сообщение AI-помощнику

**Тело запроса:**
```json
{
  "message": "Как оформить договор с поставщиком?",
  "category": "legal",
  "context": {}
}
```

**Ответ:**
```json
{
  "response": "Ответ AI...",
  "category": "legal",
  "suggestions": ["Вопрос 1", "Вопрос 2", "Вопрос 3"]
}
```

### POST `/analyze-data`
Анализ операционных данных бизнеса

**Тело запроса:**
```json
{
  "sales": 100000,
  "expenses": 80000,
  "profit": 20000
}
```

**Ответ:**
```json
{
  "analysis": "Анализ данных..."
}
```

### POST `/generate-content`
Генерация маркетингового контента

**Тело запроса:**
```json
{
  "type": "post",
  "topic": "Новая коллекция",
  "tone": "дружелюбный",
  "length": "короткий"
}
```

**Ответ:**
```json
{
  "content": "Сгенерированный контент...",
  "type": "post"
}
```

## Особенности

- ✅ Express.js с ES6 модулями
- ✅ Валидация запросов с express-validator
- ✅ Обработка ошибок
- ✅ Fallback на GPT-3.5-turbo при ошибках GPT-4
- ✅ Логирование запросов
- ✅ CORS настроен для frontend

## Переменные окружения

- `OPENAI_API_KEY` - API ключ OpenAI (обязательно)
- `PORT` - Порт сервера (по умолчанию 8000)
- `NODE_ENV` - Окружение (development/production)
